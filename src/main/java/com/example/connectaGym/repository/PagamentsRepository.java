package com.example.connectaGym.repository;

import com.example.connectaGym.entity.Pagaments;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PagamentsRepository extends JpaRepository<Pagaments, Long> {
}
